kind = "mobile"
previewPath = "/mobile/"
title = "TryNex Mobile"
version = "1.0.0"
id = "artifacts/trynex-mobile"
router = "expo-domain"

[[integratedSkills]]
name = "expo"
version = "1.0.0"

[[services]]
name = "expo"
paths = [ "/mobile/" ]
localPort = 8084

[services.development]
run = "pnpm --filter @workspace/trynex-mobile run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/trynex-mobile", "run", "build" ]
run = [ "pnpm", "--filter", "@workspace/trynex-mobile", "run", "serve" ]

[services.env]
PORT = "8084"
BASE_PATH = "/mobile/"
