
## Full public API audit after backend propagation

At 23:35 UTC, the fresh browser session showed that the Render 2 backend fix has now propagated. The public `/api/mockups` route returned HTTP 200 through `trynex-api-standby-2.onrender.com` with **108 canonical smart-v4 rows**, including `tshirt/white/front` and `tshirt/white/back`. The public `/api/products?limit=100` returned 70 products; `/api/categories` returned 7 categories; `/api/blog` returned 5 of 21 posts; `/api/products/80` returned a complete product detail object; `/api/public-stats` and `/api/settings` returned HTTP 200.

The same audit found two route-contract defects. `/api/health/readiness` and `/api/health/liveness` returned HTTP 200 through Render 2, but `/api/healthz` returned HTTP 503 with `api_unavailable`, and `/api/readyz` returned HTTP 404 with `not_found`. Because the gateway allowlist and codebase refer to both legacy and modern health paths, these defects must be reconciled before the public health contract can be classified fully verified.

The page currently had 18 images, 14 loaded at the time of the probe, and zero failed images among completed image elements; visible hero and category assets were smart-v4 1024x1024 PNGs. No visible homepage error text was found in this session.

## Direct Render 2 health discrimination

A direct browser probe of Render 2 at 23:37:53–23:37:56 UTC returned HTTP 200 for `/api/healthz`, `/api/health/readiness`, and `/api/health/liveness`. The direct healthz body reported `status=ok`, `db=ok`, `redis=ok`, `storage=r2`, `runtimeRole=standby`, `schedulerEnabled=false`, and `backupSyncEnabled=false`. The earlier public `/api/healthz` 503 was therefore a transient gateway/cold-start observation, not a persistent backend dependency failure. `/api/readyz` remains a genuine HTTP 404 because no alias route exists.

## Post-merge gateway and Render 3 verification

At 23:47 UTC, the public homepage rendered normally. Public `/api/healthz`, `/api/health/liveness`, `/api/health/readiness`, `/api/products`, and `/api/mockups` all returned HTTP 200 through Render 2. The public mockup response contains 108 smart-v4 canonical rows and the products response contains all 70 products.

Render 3 direct probes returned HTTP 200 for `/api/health/readiness` with `runtimeRole=dr`, `db=true`, scheduler disabled, backup sync disabled, and `/api/mockups` with 108 rows. This confirms Render 3 is also live and synchronized for the canonical mockup/data contract.

The new `/api/readyz` alias returned HTTP 404 both publicly through Render 2 and directly on Render 3. The health-alias merge is complete in GitHub main, but the current Render deployment source is still the earlier hardening build; Render 2/3 have not yet received the alias. This is the remaining synchronization defect to resolve.

## Keyword-audit customer surfaces

A fresh homepage visit at 23:52 UTC rendered the navigation, six family hero tiles, category links, offers, Design Studio CTA, social/contact links, and no visible fatal error.

The first `/products` screenshot briefly showed the expected client-side loading state with a blank product region. After one settle/view cycle, the page rendered the complete catalogue. The UI displayed `All Products 70`, `Showing 50 products`, categories for Caps, Custom Orders, Hoodies, Long Sleeves, Mugs, T-Shirts, and Water Bottles, search, sort, price filters, availability filter, grid/list controls, and Previous/1/2/Next pagination. Visible cards included products from bottles, long sleeves, caps, mugs, hoodies, and T-shirts with loaded images, prices, discounts, and add-to-cart controls. No catalogue error was reproduced after the normal loading window.

The T-Shirts filter route rendered **13 products** after its initial skeleton period, including product cards, discounts, colors, add-to-cart controls, and loaded local/external image URLs. The filtered count and visible card set matched the page content; no runtime error was visible after settling.

The Long Sleeves filter route returned **10 products** with matching long-sleeve image paths, prices, discounts, and product cards in the page content. The screenshot was captured during the normal skeleton/loading frame, so a settle check remains desirable for visual classification, but the route data and controls are present.

The Hoodies route reported **12 products** and the Mugs route reported **12 products**. Both returned complete product metadata in the page content, including prices, discounts, family-specific image paths, and standard shop controls. Both screenshots captured the expected initial loading/skeleton frame; the page content confirmed the filtered datasets were available rather than an API error.

The Caps filter route returned **12 products**, and the Water Bottles filter route returned **10 products**. Both page contents included family-specific product names, image paths, prices, discounts, and shop controls. Initial screenshots showed skeleton loading, but the route data was present; final visual settle checks are still required for those two pages before assigning full visual verification.

A settle check for Water Bottles confirmed all **10 bottle cards** visibly rendered with loaded bottle imagery, discounts, prices, color dots, wishlist controls, and add-to-cart controls. The bottle family is visually operational on the catalogue surface.

Product-detail audit: the T-Shirt `birthday-celebration-tee` page rendered a 4.9-rated product with 25% advance messaging, two fit choices, S–XXL sizes, four color choices, stock, image, add-to-bag, WhatsApp, details/reviews, bundle, and related-product surfaces. The Long Sleeve `cozy-waffle-knit-long-sleeve` page rendered a long-sleeve image, S–XXL size controls, color choices, pricing/discount, add-to-bag, WhatsApp, bundle, related and recently viewed surfaces. Neither page showed a fatal error.

The Hoodie detail route rendered a visible hoodie image, S–XXL sizes, four color choices, price/discount, 25% advance terms, stock-related product content, add-to-bag, WhatsApp, bundle, related, and recently viewed surfaces. The Mug detail route rendered a visible ceramic mug image, four style choices (General Mug, Love Shape Handle, Blue Rim, Yellow Rim), color controls, pricing, add-to-bag, WhatsApp, bundle, related, and recently viewed surfaces. No fatal error was reproduced.

The Cap detail route rendered a cap image, one-size product content, four color choices, price/discount, 25% advance terms, add-to-bag, WhatsApp, bundle, related, and recently viewed sections. The Water Bottle detail route rendered a bottle image, one-size content, four color choices, price/discount, 25% advance terms, add-to-bag, WhatsApp, bundle, related, and recently viewed sections. Both routes were customer-visible and did not show a fatal error.

Design Studio V2 audit: `/design-studio` loaded the editor with Print Zone, 3D Preview, Add to Cart, Select/Text/Shape/Draw/Pick color, undo/redo/zoom/reset, print-zone and texture toggles, export, Front/Back/L.Sleeve/R.Sleeve/Neck views, garment colors, upload, Text, AI Art, Layers, Templates, QR, size controls, and PNG export. Opening the product picker showed all six live families: Unisex T-Shirt, Unisex Long Sleeve, Unisex Hoodie, Coffee Mug, Structured Cap, and Water Bottle, each with smart-v4 preview imagery. Selecting Hoodie switched the route to `?product=hoodie`; switching to Back changed the route to `?product=hoodie&view=back` and visibly rendered a hoodie back mockup. The editor was not blank, although the overlay correctly prompts the user to upload or add artwork before designing.

The Design Studio picker was reopened and Water Bottle was selected. The editor switched to `?product=waterbottle`, displayed `Water Bottle 600ml Aluminium`, rendered a visible white bottle mockup, and exposed bottle-specific color choices (White, Black, Navy, Forest, Sky Blue, Red, Pink, Teal), upload, text, AI Art, Layers, Templates, QR, and PNG export. This confirms the previously reported water-bottle blank/switching issue is not reproduced on the current public bundle.

Design Studio cart regression: clicking Add to Cart with no artwork correctly produced a `No design — Add an image or text layer first` guard. Opening Text and clicking Add Text Layer created one visible text layer (`Double click to edit`) on the bottle. Clicking Add to Cart then succeeded with `Custom Water Bottle (White) is ready`, opened the cart view, displayed a custom preview, and incremented the header cart count to 1. No order was submitted.

Cart and checkout audit: `/cart` preserved the custom Water Bottle preview, white variant, one quantity, subtotal ৳998, shipping ৳100, total ৳1,098, remove/re-edit controls, and Checkout. `/checkout` preserved the item and exposed Sign in, Create account, Continue as Guest, first/last name, email, phone, address, delivery-area selection, order notes, promo code, and payment summary. Clicking Continue to Payment with empty fields correctly blocked progression and displayed required-name, phone-format, street-address, and district validation errors. No payment or order was submitted.

The Blog route rendered Trynext Magazine with 21 articles, category filters, search, newest/popular sorting, top posts, featured/latest cards, images, and article links. The FAQ route rendered category filters for delivery, returns, sizing, custom orders, payments, and accounts, plus 18 visible question controls and WhatsApp support. No fatal error was reproduced on either content surface.

The About route rendered the brand story, mission, Bangladesh manufacturing narrative, team, contact links, and collection/design CTAs. The Referral route rendered the 10% credit program, name/email/phone form, explanation, FAQ, and signup CTA. The referral form was not submitted, so code-generation backend behavior remains unverified.

The Hampers route visibly states `No curated hampers yet — try Build Your Own above`, which is an explicit partial-data state rather than a hidden error. The Build Your Own route loaded a product selection matrix across tees, bottles, long sleeves, caps, mugs, and hoodies, plus recipient name, gift message up to 200 characters, a 3-item threshold, automatic 5% bundle discount, and total calculation. The initial screenshot was still showing product skeleton cards, so a settled selection/addition test remains pending.

The Track Order route rendered a live tracking form requiring order number and email/phone, plus nationwide-delivery and WhatsApp support content. The Size Guide route rendered T-Shirt/Polo and Hoodie/Sweatshirt measurement tables with S–3XL, fit tips, shrinkage guidance, and WhatsApp sizing support. No lookup or order data was submitted.

The Shipping Policy route rendered 64-district coverage, zone-based prices, free shipping above ৳1,500, processing/ready-made timelines, tracking link, 25% advance and balance-on-delivery guidance. The Return Policy route rendered a 3-day window, eligible and ineligible cases, return workflow, and bKash/Nagad/uPay refund guidance. Both routes were visibly rendered without fatal errors; operational fulfillment/refund execution remains unverified because no real order was created.

The Privacy Policy route rendered collection/use/sharing/security/rights sections and contact links, including Google Analytics/Facebook Pixel claims and a last-updated date of April 7, 2026. The Terms route rendered general use, product pricing, 25% advance/75% COD balance, custom-order cancellation, shipping, returns, IP, liability, changes, and contact sections. Both policy pages rendered cleanly. Their operational/legal truthfulness is a content-governance review item rather than a runtime defect.

Fresh browser API probes at approximately 00:08–00:09 UTC completed successfully. Public `/api/readyz` returned HTTP 200 with `db=true`, `runtimeRole=standby`, `schedulerEnabled=false`, and `backupSyncEnabled=false`, through `trynex-api-standby-2.onrender.com`. Public `/api/healthz` returned HTTP 200 with `db=ok`, `redis=ok`, `storage=r2`, and the same standby controls. `/api/products?limit=100` returned HTTP 200 with 70 products through Render 2. `/api/mockups` returned HTTP 200 with 108 canonical smart-v4 rows; the first rows used `/mockups/smart-v4/tshirt/white/front.png?v=smart-v4` and corresponding back/front keys. OPTIONS `/api/products` returned HTTP 204 with the expected Cloudflare CORS headers. A non-submitting `POST /api/orders` remained pinned to `trynex-api.onrender.com` and received Render's HTTP 503 Service Suspended page; it was not replayed to a standby and created no order.

Source/asset scan: no `source-kit-v3` references were found outside the archived `public/mockups/source-kit-v3/manifest-index.json`; active storefront, gateway, and API runtime code use `smart-v4`. No `apparel-v5` references exist in the current runtime tree, so the prior apparel-v5 provenance request is not implemented or evidenced. The local smart-v4 PNG tree contains 16 cap, 50 hoodie, 50 long-sleeve, 30 mug, 40 T-shirt, and 16 water-bottle PNG files; no duplicate product-image basenames were found in `public/assets/products`. This is not proof of photographic realism or PSD/PSB master provenance.

Local smart-v4 integrity scan found no zero-byte PNGs. Family counts are cap 16, hoodie 50, longsleeve 50, mug 30, tshirt 40, and waterbottle 16. Representative files are valid 1024×1024 8-bit RGBA PNGs. This verifies file integrity and coverage at the local asset level, but does not establish that every front/back pair is photorealistic, geometrically matched, or backed by layered PSD/PSB masters.

Anonymous `/admin` boundary check: the public route served the application shell with loading/refresh messaging and did not expose dashboard data, orders, or operational controls. The browser session reset to `about:blank` while waiting, so authenticated admin operations remain **UNVERIFIED** rather than claimed complete; no credentials or protected mutation were attempted.

Custom-domain check: `https://trynext.pages.dev/` returned HTTP 200 from an Nginx origin with a July 30, 2026 static HTML last-modified timestamp, which differs from the current Cloudflare Pages release and therefore requires alias-parity review. `https://trynext.pages.dev/` could not be reached from the sandbox because of `SSL_ERROR_SYSCALL`; this is **BLOCKED** for independent shell/browser confirmation. The pages.dev hostname remains the verified customer release path.

Domain parity investigation: `pages.dev` serves Cloudflare with current hashed bundle `assets/index-t4DxMMEC.js` and current storefront HTML. The apex `trynext.pages.dev` serves a separate 4,730-byte Nginx HTML document with stale bundle `assets/index-BuG04mTK.js` and no current Cloudflare headers. This is a **VERIFIED production gap**: the custom apex domain is not mapped to the same current Pages release. The site is therefore current on pages.dev but not A-to-Z current at trynext.pages.dev.

Tracked-source credential scan: no filenames matched the audited Render token, OpenAI-style key, AWS access-key, private-key, or literal R2 secret patterns. This is a pattern-based scan, not a substitute for provider secret rotation or Git history scanning.

Cloudflare control-plane audit: the Pages project lists `trynext.pages.dev` as `active`, while `trynext.pages.dev` is attached but `pending` with HTTP validation still pending. The connected Cloudflare account has no managed DNS zone for `trynext.pages.dev`; independent DNS resolution points both apex and www to `104.219.250.37` and `2.59.170.20`, which serve the stale Nginx site. The verified Pages hostname is therefore operational, but the apex custom domain requires external DNS/hosting correction before it can serve the current Pages release.

Legacy asset footprint: `public/mockups/source-kit-v3` still ships **56 MB** across 217 public files (108 PNGs and 109 JSON manifests). Active runtime code uses smart-v4, but the old source-kit-v3 set remains publicly deployable and contains many self-referential legacy paths. This is a **PARTIAL/optimization gap**, not an active rendering failure; removal should be done in a reviewed cleanup PR after confirming no V1 compatibility requirement remains.

Final local gates on commit `019e0eb03` passed: workspace declaration generation, API typecheck, API tests, API build, storefront typecheck, storefront build, and gateway tests. The storefront build emits a performance warning for several chunks above 500 kB and reports a PWA precache of 218 entries totaling approximately 341,909 KiB. This is a **PERFORMANCE IMPROVEMENT** item and a likely contributor to slow first load, but it did not fail the release gates.

Safe performance fix implemented locally: `vite.config.ts` now excludes `dist/assets/products/**` from PWA precache because `src/sw.ts` already applies bounded runtime image caching. The rebuilt service worker precache dropped from approximately 341,909 KiB across 218 entries to **16,513.89 KiB across 128 entries**. Storefront build passed; the remaining warning is limited to a few large JavaScript chunks. This fix is not yet production-deployed.

Post-merge production verification: `https://trynext.pages.dev/api/readyz` continues to return HTTP 200 with `db=true`, `runtimeRole=standby`, `schedulerEnabled=false`, and `backupSyncEnabled=false`. However, cache-busted retrieval of production `/sw.js` still exposes product-image precache entries, so the PWA optimization is **MERGED BUT NOT YET VERIFIED LIVE** on the production hostname. The ad hoc Pages deployment `5a8a2c84` was accepted and queued, but its public preview is behind Cloudflare Access and the production hostname has not yet switched to its optimized worker.

Latest production recheck after Pages deployment: cache-busted `/sw.js` extraction contains no actual `url: "products/` precache entries, confirming the PWA optimization is now **VERIFIED LIVE** on the production pages.dev hostname. `/api/products?limit=100` remains HTTP 200 with 70 products, and `/api/mockups` remains HTTP 200 with canonical smart-v4 paths and 108 rows. The deployment preserved the public data contracts while reducing install-time cache scope.
